package edu.skku.cs.finalproject;

import static edu.skku.cs.finalproject.MainActivity.micIcon;
import static edu.skku.cs.finalproject.MainActivity.micOffIcon;
import static edu.skku.cs.finalproject.MainActivity.pauseIcon;
import static edu.skku.cs.finalproject.MainActivity.playArrowIcon;

import android.widget.Button;

import androidx.databinding.BindingAdapter;

public class BindingAdapters {
    /*
    @BindingAdapter("android:recording")
    public static void setRecordIcon(Button btn, Boolean isRecording){
        if (isRecording) btn.setCompoundDrawables(null, micOffIcon, null, null);
        else btn.setCompoundDrawables(null, micIcon, null, null);
    }

    @BindingAdapter("android:playing")
    public static void setPlayIcon(Button btn, Boolean isPlaying){
        if (isPlaying) btn.setCompoundDrawables(null, pauseIcon, null, null);
        else btn.setCompoundDrawables(null, playArrowIcon, null, null);
    }

     */
}
